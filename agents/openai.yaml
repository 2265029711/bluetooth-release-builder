interface:
  display_name: "蓝牙版本发布助手"
  short_description: "用于蓝牙嵌入式版本差异修改、快速编译与修改位置汇总处理"
  default_prompt: "使用 $bluetooth-release-builder，并优先运行 scripts/bootstrap_context.py 判断下一步。若修改记录已命中，且我已明确给出宏值或参数内容，就直接修改并编译，不要重复确认；同一文件里的多处修改请一次性处理；最后只汇总修改了哪些位置、改了什么以及编译结果。"
